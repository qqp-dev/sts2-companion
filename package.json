{
  "name": "@hypermemetic-ai/sts2-companion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phone-first, read-only Slay the Spire 2 static combat companion for qq.",
  "main": "src/plugin.mjs",
  "exports": {
    ".": "./src/plugin.mjs",
    "./book": "./src/book.mjs",
    "./state": "./src/state.mjs",
    "./http": "./src/http.mjs",
    "./cordis.patch.yml": "./cordis.patch.yml"
  },
  "files": [
    "src/",
    "data/",
    "docs/",
    "cordis.patch.yml",
    "README.md"
  ],
  "scripts": {
    "test": "node --test && python3 -m unittest discover -s test -p 'test_*.py'",
    "build:encounter-facts": "python3 tools/generate-encounter-facts.py",
    "check:encounter-facts": "python3 tools/generate-encounter-facts.py --check"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  }
}
